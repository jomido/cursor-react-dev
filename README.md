## develop (local)

- `npm run install`
- `npm run dev:full`

Ideally, development is dockerized as well.

## production (dockerized) 

- `./build.sh`
- `./run.sh`
- `./stop.sh`

## testing

- `npm run test`
